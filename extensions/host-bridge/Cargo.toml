[package]
name = "boomslang-ext-host-bridge"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
pyo3 = { version = "0.26.0", default-features = false, features = ["macros"] }

[build-dependencies]
boomslang-hostgen = { path = "../../boomslang-hostgen" }
